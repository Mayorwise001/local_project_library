<!DOCTYPE html>
<html lang="en">
<head>

    <%-include ('header.ejs') %>
</head>
<body>
    <%- include ('layout.ejs') %>
    <div class='col-sm-10'>
  <h1><%= title %></h1>

  <form action="<%= (genre && genre._id) ? '/catalog/genre/' + genre._id + '/update' : '/catalog/genre/create' %>" method="POST">
    <div class="form-group">
      <label for="name">Genre:</label>
      <input id="name" class="form-control" type="text" placeholder="Fantasy, Poetry etc." name="name" required  value>
    </div>
    <button class="btn btn-primary" type="submit">Submit</button>
  </form>
</div>
</body>
</html>
